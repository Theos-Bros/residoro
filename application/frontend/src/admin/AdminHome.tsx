export function AdminHome() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Welcome</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        This is the operator dashboard shell. Enrollment, contract lifecycle, export, and
        training screens land here in later tracer bullets.
      </p>
    </div>
  );
}
