PORT=4000

# "Rdoro Demo" Supabase project — safe to commit, it's just the project's public URL.
SUPABASE_URL=https://ngizypfgpynsvijiopji.supabase.co

# Secret — get this from the Supabase dashboard (Project Settings > API).
# Never commit the real value. This key bypasses Row-Level Security.
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key
