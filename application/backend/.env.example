PORT=4000

# "Residoro Prototype" Supabase project — safe to commit, it's just the project's public URL.
SUPABASE_URL=https://skfnrcwqvmurnpwrmixj.supabase.co

# Secret — get this from the Supabase dashboard (Project Settings > API).
# Never commit the real value. This key bypasses Row-Level Security.
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key

# Secret — used by the migration field-mapping endpoint (tb-migration-csv-001).
# Get a free key from aistudio.google.com/apikey (Google AI Studio, no billing required).
# Never commit the real value.
GEMINI_API_KEY=replace-with-gemini-api-key
